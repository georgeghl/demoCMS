<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="top.clarkhg.democms.mapper.AdminUserMapper">
    
<!--     
    <insert id="insertAdminUser" parameterType="top.clarkhg.img_viewer.dao.AdminUser">
        INSERT INTO user VALUES( #{username}, #{password}, #{email})
    </insert>

    <update id="updateAdminUserPassword" parameterType="top.clarkhg.democms.dao.AdminUser">
        UPDATE user SET password=#{password} WHERE username=#{username}
    </update>

    <select id="selectAdminUserByUsername" resultType="top.clarkhg.democms.dao.AdminUser">
        SELECT
            user.username AS username,
            user.password AS password
        FROM
            user
        WHERE
            user.username = #{username}
    </select>
 -->

</mapper>